import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ph2.u2.carlosvega',
  appName: 'ph2-u2-carlosvega',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
